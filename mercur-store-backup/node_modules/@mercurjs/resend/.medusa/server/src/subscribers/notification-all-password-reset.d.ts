import { SubscriberArgs, SubscriberConfig } from "@medusajs/framework";
export default function passwordResetHandler({ event, container, }: SubscriberArgs<{
    entity_id: string;
    actor_type: string;
    token: string;
}>): Promise<void>;
export declare const config: SubscriberConfig;
