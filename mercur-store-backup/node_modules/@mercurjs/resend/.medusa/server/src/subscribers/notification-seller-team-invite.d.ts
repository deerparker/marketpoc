import { SubscriberArgs, SubscriberConfig } from "@medusajs/framework";
export default function sellerTeamInviteHandler({ event, container, }: SubscriberArgs<{
    user_name: string;
    store_name: string;
    token: string;
    id: string;
    email: string;
}>): Promise<void>;
export declare const config: SubscriberConfig;
