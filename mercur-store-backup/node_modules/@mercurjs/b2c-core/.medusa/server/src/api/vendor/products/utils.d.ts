import { MedusaContainer } from '@medusajs/framework';
type OrderDirection = 'ASC' | 'DESC' | 'asc' | 'desc';
export type OrderObject = Record<string, OrderDirection | Record<string, OrderDirection>>;
export declare const filterProductsBySeller: (container: MedusaContainer, sellerId: string, skip: number, take: number, salesChannelId?: string, order?: OrderObject) => Promise<{
    productIds: any[];
    count: number;
}>;
export {};
